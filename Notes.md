# Console Calculator — Notes

- Goal: consolidate parsing, validation, and evaluation with clean separation of responsibilities.
- Program.cs must orchestrate only (no business logic).
- Input should support both `12+3` and `12 + 3`.